# Dependencias verificadas para Fedora (probado en Fedora 44, Python 3.14)
# Diferencias frente a requirements.txt (Windows/Python 3.11):
#   - pygame  -> pygame-ce : la rueda oficial de pygame no tiene binarios
#                completos para Python 3.14 (falta el modulo mixer). pygame-ce
#                es un reemplazo directo (se importa como "pygame") con audio.
#   - numpy<2 -> numpy 2.x : numpy 1.26 no tiene ruedas para Python 3.14.
#                El juego solo usa APIs basicas, compatibles con numpy 2.
mpmath==1.3.0
numpy==2.5.1
pillow==12.3.0
pygame-ce==2.5.7
PyOpenGL==3.1.10
sympy==1.14.0
